name: Smoke Test

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

defaults:
  run:
    shell: bash

jobs:
  smoke-test:
    name: Build CLAP plugin
    runs-on: macos-14
    env:
      MACOSX_DEPLOYMENT_TARGET: "11.0"
      WRY_OBJC_SUFFIX: WracGainPlugin

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.12.0"
          cache: npm
          cache-dependency-path: src-gui/package-lock.json

      - name: Show Rust version
        run: |
          rustup show
          rustc --version
          cargo --version

      - name: Check workspace
        run: cargo check --locked --workspace

      - name: Build CLAP bundle
        run: cargo xtask build --release --target=clap

      - name: Verify CLAP bundle
        run: test -d "target/wrac/artifacts/release/WRAC Gain.clap"
